services:
  ai-agent:
    build: .
    container_name: ibhelm-ai-agent
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Europe/Berlin
    networks:
      - supabase_default

networks:
  supabase_default:
    external: true

